<Page
    x:Class="TwitchUWP.VideoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TwitchUWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:TwitchUWP.Models"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Viewbox Stretch="Uniform" StretchDirection="Both"  Grid.Row="0">
            <MediaElement Name="StreamPlayer" Width="640" Height="360" RealTimePlayback="True" PosterSource="Assets/VideoPreLoad.png" />
        </Viewbox>
        <ScrollViewer Grid.Row="1" Name="ChatScrollViewer">
            <StackPanel Margin="10" Name="ChatMessageView">
                <!-- Chat messages go in here... -->
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
